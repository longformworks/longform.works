# 长作 Longform Works · 设计说明

站主的一人软件工作室门面站。本文记录已经定稿的设计决策，改动前先读。

## 身份

- 名字：中文「长作」（谐音「常做」），西文 Longform Works。两者并列，中文为主：大字「长作」，小字 Longform Works。
- 印章：红底方印，白字「长」。字形来自思源宋体 SemiBold，转成 SVG 路径（`src/generated/glyphs.json`），不依赖访客机器上的字体。
- 「一字一作」：以后每个作品也用「深色方块 + 单字」做图标，与印章同一图形语言。

## 文案（定稿）

- 只陈述事实，不写口号、不自我介绍、不说「用心」「打磨」之类的话。
- 首页只有一句：中文「长作，常做。」，英文「Long-term projects, worked on a little at a time.」
- 页脚：「由 Alvie Zhang 制作。」（链到 alvie.me）与 GitHub。没有版权行，没有字体说明。
- 「Longform Works」在页面上只出现一次（大标题下），浏览器标题固定为「长作 Longform Works」，切换语言不改标题。
- 全部文案在 `src/i18n/index.ts`，中英各一份，结构完全对应。

## 版式（定稿）

- 页眉：左侧 32px 印章（回首页），右侧 EN / 中文 双段胶囊开关；有作品时开关左边出现「作品」链接。
- 首页桌面端（≥ 64em）：左栏名字在顶、那句话在底；右栏 26rem 正方形胭脂红方块，里面的「长」放大到 150%、向右下溢出边框（`width:150%; right:-38%; bottom:-42%`）。这是站主指认的版本，不要缩小、不要居中、不要改裁切。
- 方块在任何视口都必须是正方形（`aspect-ratio: 1 / 1`，最大 26rem）。曾经让它随视口拉伸，「长」的裁切就跑偏，站主报告「长会被覆盖」。
- 640–1023px：上下堆叠，方块左对齐、最宽 26rem。手机：名字、句子、方块依次排列，方块占满宽度。
- 页脚贴底：内容短时页脚在视口底部。
- 作品在独立的 `/works` 页，列表空时只显示一句「还没有发布的作品。」

## 动效（定稿）

- 只有落印：方块里的「长」和页眉小印，页面加载后落一次，切换语言时再落一次。
- 参数：`scale 1.25 → 0.985 → 1`，`rotate -3° → 0.4° → 0`，640ms，延迟 200ms，缓动 `cubic-bezier(0.2, 0.9, 0.3, 1)`；变换原点 `33% 46%`（笔画交叉处附近），墨色在 28% 处到位。
- 尊重 `prefers-reduced-motion`。
- 站主明确表示喜欢这一版，不要缩短、简化或去掉。

## 颜色（定稿）

| 用途 | 浅色 | 深色 |
|---|---|---|
| 纸底 `--paper` | #EEF1F5 | #101624 |
| 悬停面 `--hover` | #E4E9F1 | #192235 |
| 墨 `--ink` | #0B1220 | #E8EDF5 |
| 次级 `--ink-soft` | #526079 | #A6B1C5 |
| 分隔线 `--rule` | #CBD2DE | #344056 |
| 印泥 `--seal` | #A31F34 | #C63A4C |
| 印上的字 `--seal-ink` | #F4F6FA | #F4F6FA |

冷灰白纸底、靛墨、胭脂红。刻意避开暖米色 + 陶土红的组合。

## 字体（定稿）

- 品牌名：西文 Spectral 400，中文思源宋体 400（子集）。
- 正文与界面：西文 Source Sans 3 400 / 500，中文思源黑体 400 / 500（子集）。Adobe 配套的一对。
- 全部自托管，不使用系统字体；字体栈只留通用回退。
- 中文子集只含文案与作品数据中字符串里出现的字，由 `scripts/subset-fonts.mjs` 生成，文件名带内容哈希，`public/_headers` 给 `/fonts/*` 与 `/_astro/*` 一年缓存。品牌用的三个文件在 `<head>` 预加载。
- 中文排版：行高 1.75，全角标点，`line-break: strict`，中西文之间加空格，不用斜体。

## 双语

- 一个页面里渲染中英两份正文，`html[data-lang]` 决定显示哪份；首屏前的内联脚本按 localStorage，其次按浏览器语言决定。
- 不做 `/zh` 独立路由（站主否决）。代价是无 JavaScript 时只有英文。

## 部署

- 推 `main` → GitHub Actions → `wrangler deploy` 到 Cloudflare Workers 静态资源。
- `worker/index.ts` 把 `longform.work` 与 `www.*` 301 到 `longform.works`；`run_worker_first: true` 保证跳转先于静态资源。
